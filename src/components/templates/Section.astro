---
import { cn } from '@/lib/utils'

interface Props {
    className?: string
}

const { className } = Astro.props
---

<section class={cn('flex flex-col space-y-4 py-[2rem]', className)}>
    <slot />
</section>
